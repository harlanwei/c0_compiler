/**
 * A test function.
 */
int fn() {
    int i = 0;
    int limit = 10;
    scan(i);
    while (i < limit) {
        print(i, i * 2);
        i = i + 1;
    }
    return i;
}

void main() {
    int result = fn();
    print(result);
    return;
}
